# v0.35-r2 real deep materialization

This correction replaces the former landing-page-only implementation. Revision r2 executes each configured official endpoint, records raw response evidence, parses HTML, JSON and PDF responses, and derives final segment state only after the probe result.

The candidate ledger contains landing pages plus direct official BMV, KRX API and JSE assets. A full membership PASS requires an official reproducible response, current full-constituents semantics, parsed security rows, unique codes and a plausible full count. Change/rebalance assets never promote membership.

The immutable predecessor is WELT-SWING-CURRENT-Handoff-v0.34.md; CURRENT is not used as a frozen predecessor. Current master and research_partial_1633 remain immutable.
